import 'package:fluent_ui/fluent_ui.dart';

class DsmPage extends StatelessWidget {
  const DsmPage({super.key});

  @override
  Widget build(BuildContext context) {
    return ScaffoldPage.scrollable(
      header: const PageHeader(
        title: Text('DSM'),
      ),
      children: const [],
    );
  }
}
